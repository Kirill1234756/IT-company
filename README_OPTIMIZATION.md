# 🚀 Оптимизация производительности - Быстрый старт

## ✅ Что уже сделано

1. **Service Worker** - создан и зарегистрирован
2. **Утилиты для изображений** - созданы компоненты и утилиты
3. **Tree-shaking** - улучшена конфигурация Vite
4. **Code splitting** - оптимизирован

## 📦 Установка зависимостей

```bash
npm install -D sharp
```

## 🖼️ Оптимизация изображений

### Шаг 1: Конвертировать изображения

```bash
npm run optimize:images
```

Это создаст WebP и AVIF версии всех изображений в `public/img/` с размерами 400w, 800w, 1200w.

### Шаг 2: Использовать OptimizedImage

Пример замены в `PortfolioCard.vue`:

```vue
<script setup>
import OptimizedImage from '@/components/OptimizedImage.vue'
</script>

<template>
  <OptimizedImage
    :src="image || '/api/placeholder/400/300'"
    :alt="title"
    :width="400"
    :height="300"
    :widths="[400, 800, 1200]"
    format="webp"
    :sizes="{
      mobile: '100vw',
      tablet: '50vw',
      desktop: '33vw',
    }"
    loading="lazy"
    fetchpriority="low"
    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500 rounded-[3rem]"
    style="box-sizing: border-box; display: block; transform: translateZ(0)"
  />
</template>
```

## 📊 Проверка результатов

1. **Соберите проект:**

   ```bash
   npm run build
   ```

2. **Проверьте бандл:**
   - Откройте `dist/stats.html` в браузере
   - Проанализируйте размеры чанков

3. **Проверьте в Lighthouse:**
   - Откройте DevTools → Lighthouse
   - Запустите аудит
   - Проверьте метрики

## 🎯 Ожидаемые улучшения

- **Размер изображений**: -30-50%
- **Размер JavaScript**: -20-40%
- **Performance Score**: +10-20 баллов
- **LCP**: -20-30%
- **TBT**: -30-50%

## 📝 Следующие шаги

1. Запустить `npm run optimize:images`
2. Заменить `<img>` на `<OptimizedImage>` в компонентах
3. Пересобрать проект
4. Протестировать в Lighthouse

Подробная документация: `OPTIMIZATION_GUIDE.md`





















