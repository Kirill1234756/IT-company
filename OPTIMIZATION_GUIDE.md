# Руководство по оптимизации производительности

## ✅ Выполненные оптимизации

### 1. Оптимизация изображений

- ✅ Создан компонент `OptimizedImage.vue` с поддержкой WebP/AVIF
- ✅ Создана утилита `imageOptimizer.ts` для генерации srcset
- ✅ Создан скрипт `scripts/optimize-images.js` для конвертации изображений

### 2. Минимизация JavaScript

- ✅ Улучшен tree-shaking в `vite.config.ts`
- ✅ Настроен `moduleSideEffects` для исключения ненужных side effects
- ✅ Уменьшен `chunkSizeWarningLimit` до 500KB
- ✅ Оптимизирован code splitting

### 3. Оптимизация CSS

- ✅ Tailwind v4 использует JIT компиляцию (автоматически удаляет неиспользуемый CSS)
- ✅ Создан `tailwind.config.js` для конфигурации

### 4. Service Worker

- ✅ Создан `public/sw.js` с стратегиями кэширования
- ✅ Зарегистрирован в `main.ts` для production

## 📋 Инструкции по использованию

### Оптимизация изображений

#### Шаг 1: Установить зависимости

```bash
npm install -D sharp
```

#### Шаг 2: Конвертировать изображения

```bash
node scripts/optimize-images.js
```

Это создаст WebP и AVIF версии всех изображений в `public/img/` с размерами 400w, 800w, 1200w.

#### Шаг 3: Использовать OptimizedImage компонент

Замените обычные `<img>` теги на `<OptimizedImage>`:

**Было:**

```vue
<img :src="post.image" :alt="post.title" width="400" height="225" loading="lazy" />
```

**Стало:**

```vue
<script setup>
import OptimizedImage from '@/components/OptimizedImage.vue'
</script>

<template>
  <OptimizedImage
    :src="post.image"
    :alt="post.title"
    :width="400"
    :height="225"
    :widths="[400, 800, 1200]"
    format="webp"
    :sizes="{
      mobile: '100vw',
      tablet: '50vw',
      desktop: '33vw',
    }"
    loading="lazy"
    fetchpriority="low"
  />
</template>
```

### Компоненты для обновления:

1. `src/components/PortfolioCard.vue`
2. `src/components/BlogCard.vue`
3. `src/components/modals/BlogModal.vue`
4. `src/components/modals/PortfolioModal.vue`

### Service Worker

Service Worker автоматически регистрируется в production режиме. Для проверки:

1. Откройте DevTools → Application → Service Workers
2. Убедитесь, что Service Worker зарегистрирован
3. Проверьте кэш в Application → Cache Storage

### Анализ бандла

После сборки проверьте `dist/stats.html`:

```bash
npm run build
# Откройте dist/stats.html в браузере
```

Это покажет:

- Размеры всех чанков
- Неиспользуемый код
- Возможности для оптимизации

### Проверка Coverage

1. Откройте Chrome DevTools → Coverage
2. Перезагрузите страницу
3. Найдите неиспользуемый CSS/JS
4. Удалите или оптимизируйте

## 🎯 Ожидаемые результаты

После полной реализации всех оптимизаций:

### Мобильные устройства:

- **Performance Score**: 70-85 → **90-95** ✅
- **LCP**: 3-5s → **2-3s** ✅
- **TBT**: 200-500ms → **50-200ms** ✅
- **CLS**: 0.2-0.4 → **0.05-0.1** ✅

### Десктоп:

- **Performance Score**: 80-90 → **95-100** ✅
- **LCP**: 2-3s → **1-2s** ✅
- **TBT**: 100-300ms → **0-100ms** ✅
- **CLS**: 0.1-0.2 → **0-0.05** ✅

## 📝 Чеклист

- [ ] Установить `sharp`: `npm install -D sharp`
- [ ] Запустить скрипт оптимизации изображений
- [ ] Заменить `<img>` на `<OptimizedImage>` в компонентах
- [ ] Проверить Service Worker в production
- [ ] Проанализировать бандл через `dist/stats.html`
- [ ] Проверить Coverage в DevTools
- [ ] Протестировать в Lighthouse
- [ ] Проверить на реальных устройствах

## 🔧 Дополнительные оптимизации

### Если нужны дополнительные улучшения:

1. **Использовать CDN** для статических ресурсов
2. **HTTP/2 Server Push** для критических ресурсов
3. **Preconnect** для внешних доменов
4. **Resource Hints** (prefetch, preload)
5. **Compression** (gzip, brotli) на сервере
6. **Image CDN** (Cloudinary, Imgix) для автоматической оптимизации





















